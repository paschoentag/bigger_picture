# Copy to `.env` to reach the docker-compose dev stack from another device on
# your LAN (e.g. a phone, for mobile testing). Both default to localhost, which
# only works from the machine running docker compose.
FRONTEND_ORIGIN=http://192.168.10.42:5173
VITE_API_BASE_URL=http://192.168.10.42:8000
